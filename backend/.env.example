# MongoDB connection string (replace <user>, <pass>, <cluster>, <dbname>)
MONGO_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority

# Upstash Redis (if used) — REST URL and token
UPSTASH_REDIS_REST_URL=https://<your-upstash-id>.upstash.io
UPSTASH_REDIS_REST_TOKEN=<your-upstash-rest-token>

# JWT secret used to sign tokens (use a long random string in production)
JWT_SECRET=some-very-long-random-secret-here

# Google OAuth credentials (from Google Cloud Console)
GOOGLE_CLIENT_ID=<your-google-client-id>.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=<your-google-client-secret>

# Callback URL configured in Google Cloud must match this exactly
# Default assumes auth router is mounted at /api/auth
GOOGLE_CALLBACK_URL=http://localhost:5001/api/auth/google/callback

# Frontend URL used for redirects after OAuth
CLIENT_URL=http://localhost:5173

# Server port
PORT=5001

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
